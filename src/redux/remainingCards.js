import { createSlice } from "@reduxjs/toolkit";

export const remainingCardSlice = createSlice({
    name: 'remainingCard',
    initialState: {
        cardVersions: [],
        currentInput:
    }
})